<Page x:Class="ELL.Desktop.UI.Views.CoursePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ELL.Desktop.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CoursePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition />

        </Grid.RowDefinitions>
        <Label Content="Choose your course" HorizontalAlignment="Center" />
        <ComboBox ItemsSource="{Binding CourseList}" SelectedItem="{Binding SelectedCourse}"
                  MinHeight="30" MinWidth="150" Grid.Row="1"
                  HorizontalAlignment="Center" />
        <Button Content="Choose" Command="{Binding ChooseCourseCommand}" 
                Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
